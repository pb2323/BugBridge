import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure Next.js uses src directory
  // Next.js will automatically look in src/app/ if src/ exists
};

export default nextConfig;
